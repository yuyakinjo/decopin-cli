# 決定と理由 (ADR)

decopin-cli の設計で「なぜそうしたか」だけを置く。**挙動そのものは書かない** —
挙動は実装とテストが答えで、ここに書くと必ずずれる。

- 使い方 → [README.md](../README.md)
- 挙動の約束 → `test/contract/` (表はそこにしか無い)
- 個々の API の理由 → 公開シンボルの JSDoc

理由は「決めた時点の事実」なので腐らない。同じ議論を最初からやり直さないために残す。

---

## ADR 1: 自作の軽量レンダラーを使う (Ink は使わない)

JSX を再帰的に ANSI 文字列へ変換するだけで足りる。Ink は yoga-wasm を含み起動が
100ms を超え、CLI としては過剰。静的な出力に reconciler は要らない。

**代償**: 対話的 UI (プロンプト・スピナー) は自前で作ることになる。v1 では扱わない。

**関連**: `src/renderer/`。パイプラインは 評価 → レイアウト → 直列化 → 書き出し の 4 段。

## ADR 2: argv と stdin をファイルで分ける

`argv.tsx` と `stdin.tsx` を別ファイルにする。stdin は argv と性質が違う:

- 読むと**入力が来るまでブロックする** (端末で実行するとフリーズする)
- 実行時に有無が変わる (パイプ / リダイレクト / 端末)
- 一度しか読めない

「このコマンドは stdin を読む」という宣言を必須にし、宣言がないコマンドは stdin に
一切触らない。ファイルの有無でそれを表すのが、規約ベースでは一番分かりやすい。

## ADR 3: ファイル名は POSIX 用語で統一する

`argv.tsx` / `stdin.tsx` / `command.tsx` (stdout) / `error.tsx` (stderr) が
シェルの入出力と 1:1 対応する。シェルを学びながら書く人にとって、
「今書いているファイルはシェルのどの口か」が名前で分かる利点が大きい。

`params.tsx` のような一般語は、環境変数や設定ファイルの値も含みうる曖昧さがある。

## ADR 4: v1 は静的出力のみ

装飾 (色・枠・表) の表現力に投資し、対話性は後回しにする。1 回描画して終了する
出力に絞ると、パイプ・リダイレクトとの相性も良い。

**その後**: 出力側の動的な領域は ADR 22 で導入した。この ADR で守り続けるのは
「対話的な**入力**を読まない」の部分 (readline / setRawMode を持たない)。

## ADR 5: ビルド時にルートを生成する

`app/` を走査して route manifest とエントリを生成し、単一の `dist/index.js` にする。
起動が最速で、配布も 1 ファイルで済む。実行時にディレクトリを走査しない。

## ADR 6: バリデーションは valibot

軽量 (zod の約 1/10) で tree-shaking が効き、起動時間に有利。Standard Schema 準拠。

## ADR 7: `layout.tsx` を導入する

上位ディレクトリの layout が子コマンドの出力を包む。共通のテーマやヘッダを
一箇所で定義できる。Next.js とのアナロジーが完成する。

## ADR 8: `--help` は宣言から自動生成する

`argv.tsx` / `stdin.tsx` の宣言を唯一の情報源にする。スキーマと help の
二重管理が起きない。足りない場合だけ `help.tsx` で上書きする。

## ADR 9: 型はビルド時の codegen で配る

**JSX 式は型引数を運べない。** TypeScript 7.0.2 で実測した:

```tsx
declare function Option<N extends string, T>(p: {
  name: N;
  type: T;
}): Node<{ name: N; value: T }>;
const a = <Option name="loud" type={true as boolean} />;
//    ^? any (JSX.Element 未宣言時) / JSX.Element (宣言時) — 型引数は消える
```

自作 jsx-runtime で `jsx()` factory の戻り値をジェネリックにしても変わらない。
props 方式でも children 方式でも同じなので、**JSX で型を運ぶ設計は選択肢にならない**。

代わりに Next.js の typed routes と同じく、ビルド時に `.decopin/types.d.ts` を生成し、
module augmentation で `Routes` / `EnvVars` を埋める。`command.tsx` は
`CommandProps<'hello'>` で引く。

**代償**: `decopin dev` を回していないと型が古い。未生成のときは緩い型に
フォールバックするが、「古い」と「未生成」は区別できていない。

## ADR 10: schema は `Type.*` の children で組み、valibot は露出しない

利用者が valibot の書き方を覚えなくて済むようにする。`Type.Array` のように
型が再帰する場合に children の入れ子が自然に効く。valibot への依存は
`src/validation/` に閉じ込める。

**valibot に変換層を寄せなかった理由** (実測):

| 試したこと                                                     | 結果                                                                                   |
| -------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| `v.pipe(v.string(), v.transform(Number), v.number())` に `"3"` | ✅ `3` になる                                                                          |
| `v.optional(schema, 1)` に `undefined`                         | ❌ `Invalid type: Expected string but received 1` — **既定値も wrapped schema を通る** |
| `"abc"` のエラーメッセージ                                     | ❌ `Invalid type: Expected number but received NaN`                                    |
| `v.check` の message 関数で元の値を出す                        | ❌ `received null` (元の文字列が残らない)                                              |

決定的なのは 2 つ目。利用者は `default={1}` と書くのが自然なのに、内部が文字列
パイプになると数値の既定値が弾かれる。だから **変換 → 検証の 2 段構え**を維持する。
`--flag` の存在・`--no-flag`・繰り返しによる配列化も argv の性質であって型の話ではない。

**エスケープハッチ**: `<Stdin mode="json" schema={...}>` だけ生の valibot を受ける。
深い JSON を `Type.Object` / `Type.Field` で書くと 3 階層で 9 行になるため。
argv / env には用意しない (型を失う道を増やさない)。

## ADR 11: argv の検証は middleware より前

middleware が検証済みの `args` / `options` を受け取れる方が実用上の価値が大きい
(`--verbose` による分岐など)。

**代償**: env / argv の検証エラーは middleware を通らない。middleware での
ロギングや計測は「コマンドが実際に実行される場合のみ」走る。

一方 **stdin の読み取りは middleware の内側**に置く。`next()` を呼ばずに
打ち切れば標準入力を消費しないで終われる。

## ADR 12: 配布は単一ファイル + 「評価のみ」遅延

`bun build` は動的 import を同一ファイル内の遅延評価ラッパーにインライン化するため、
「実行されないコマンドを**読み込まない**」ことは実現できない。遅延されるのは
モジュール本体の**評価**のみで、トップレベルのパースは全量走る。

真の遅延読込には `--splitting` (複数ファイル配布) が必要だが、v1 は配布の単純さを取る。

**実測** (Apple Silicon / Bun 1.4、`bun run bench`):

- 総時間 12〜14ms。うち `bun` 自体のプロセス起動が 4.7ms、decopin の取り分が 7.7〜8.9ms
- **コマンド数の影響は小さい**。1 コマンドだけの app でも取り分は 6〜8ms
- `--minify` の効果は 0.4ms 程度。**パースは律速でない**
- valibot の初期化 +1.8ms、renderer 一式 +2.6ms (ソースからの import を個別に測ったもの)
- valibot を遅延 import する実験は、計測の揺れ (±1.5ms) に埋もれて差が出なかったので採用していない

目標の「10ms 未満」は decopin の取り分としては達成、総時間としては未達。

## ADR 13: middleware は `children` ではなく `next` を受け取る

`layout.tsx` の `children` は React と同じ「値」で、置くだけで済む。対して
middleware は「コマンドの実行を包んで、終わったあとに何かする」ためのものなので、
**呼ぶまで走らない関数**でなければ成立しない (時間を測る・後片付けをする・
例外を捕まえる、が全て `next()` を呼び終えた後にしかできない)。

同じ `children` という名前で「値」と「関数」の 2 種類を使い分けさせると覚えることが
増えるだけなので、React の語彙を借りず Koa / Hono / Express と同じ `next` にする。

## ADR 14: 外に出るものは英語、内側の記録は日本語

| 対象                                                    | 言語   |
| ------------------------------------------------------- | ------ |
| CLI の出力 (`--help`、エラー文、`decopin build` の出力) | 英語   |
| 生成されるファイル (`.decopin/`) のヘッダーとコメント   | 英語   |
| README                                                  | 英語   |
| コード内のコメント / このファイル / テスト名            | 日本語 |

境界は**利用者の手元に届くかどうか**。公開したときにそのまま使えることを優先する。
生成物は利用者のリポジトリに残るし、README は npm で最初に読まれる。

内側の記録を日本語のままにしているのは、書く速さと精度が保守の質に直結するため。
英日の併記はしない (2 つの文書は必ずずれる)。

## ADR 15: 仕様書を持たず、契約はテストに置く

以前は `docs/SPEC.md` (1266 行) に挙動の表・props 一覧・ファイル規約の詳細を
書いていたが、**書いてあるのに実装されていない機能が 9 件**発生した。仕様を書く側と
実装する側が別のタイミングで動くと、動かないドキュメントは必ずずれる。

そこで役割を 3 つに分けた:

| 中身           | 置き場所                        | 崩れない理由                   |
| -------------- | ------------------------------- | ------------------------------ |
| 挙動の約束     | `test/contract/` の表駆動テスト | 実行される                     |
| 使い方         | `README.md`                     | 例をテストで実行する           |
| 決定と理由     | このファイル                    | 決めた時点の事実なので腐らない |
| API 個別の理由 | 公開シンボルの JSDoc            | 実装の隣にある                 |

**この方針で捨てたもの**: 節番号による横断参照 (`§4.8` など)。代わりに ADR 番号と
テストファイルのパスで参照する。

## ADR 16: 決定を守る仕組みをテストに置く

ADR 15 で「契約はテスト、理由はここ」と決めたが、**このファイル自体が
形骸化する**という失敗の仕方が残る。性質の違う 2 つをテストで守る。

| 守りたいこと                                    | 手段                           |
| ----------------------------------------------- | ------------------------------ |
| 決定が**破られていない**                        | `test/docs/decisions.test.ts`  |
| 参照が**切れていない** (ADR 番号・テストのパス) | `test/docs/references.test.ts` |

**エディタや CLI のフックには置かない。** Claude Code のセッション中しか効かず、
人が直接編集したときや CI では働かないため。テストなら全員に効き、ずっと残る。

### ADR には守り方を必ず付ける

`test/docs/decisions.test.ts` は ADR 番号ごとに守り方を持つ。

- `lint` — このファイルの中で形を検査する (valibot の閉じ込め、メッセージの言語など)
- `test` — 別のテストが担保している (そのファイルの実在も確認する)
- `manual` — 機械では守れない。理由を書く

**ADR を足すと「分類されていない」で落ちる。** 守り方を決めるまで通らないので、
「決めたが誰も守っていない ADR」が生まれない。逆に ADR を消すと「分類が孤立して
いる」で落ちる。

### 落ちたときに直すのはコードとは限らない

決定そのものが変わったのなら、**直すのは decisions.md**。テストが「決定を
見直す」きっかけになるのが狙いで、それが ADR を生きた文書に保つ。

## ADR 17: 公開は「別ディレクトリを組み立てて」出す

npm に出すのは `publish/` に組み立てたもので、リポジトリの `package.json` は
開発用 (exports が `src/*.ts` を指す) のまま触らない。

**なぜ組み立てるか**: 開発中は自己参照 (`decopin-cli`) が `src/` を指していないと、
テストが古いビルド結果を見てしまう。かといって `exports` を `lib/` に向けると
開発のたびにビルドが要る。`publishConfig` で `exports` / `bin` を差し替える案は
**npm 11 で効かないことを実測で確認した** (`npm pack` したマニフェストが元のまま)。

**中身は tsc が出す JS + .d.ts**。ファイル構成のまま出し、バンドルしない
(利用者が一部だけ import したときに全部を読み込ませないため)。

**それでも Bun は必須**。`Bun.build` / `Bun.stdin` を呼んでいるので、JS にしても
Node では動かない。`engines.bun` で明示する。そもそも生成される CLI 自体が
`#!/usr/bin/env bun` なので、Bun 前提であることに矛盾はない。

**認証は Trusted Publishing (OIDC)**。長期の `NPM_TOKEN` を secrets に置かない。
`--provenance` で来歴も付く。タグと `package.json` の version が食い違ったら
公開前に落とす (公開してからでは取り消せないため)。

## ADR 18: バージョンは時刻そのもの (`YYYY.MMdd.HHmm`、UTC)

SemVer をやめて時刻から決める。例: `2026.828.1430` (2026-08-28 14:30 UTC)。

**なぜ**: 番号を決めるのに何も調べなくてよい。時刻だけの関数なので、
レジストリへの問い合わせも、前回の番号の記憶も要らない。単調に増え、衝突もしない。

**なぜ 4 つ組 (`yyyy.MM.dd.HHmm`) にしないか**: npm がそれを
`2026.8.2-8.1430` という**プレリリース**として解釈する (実測)。エラーにならず、
通常のインストールで拾われなくなる。semver は数値 3 つと決まっているので、
月日と時分をそれぞれ 1 つに畳む。

**先頭ゼロを付けない**。`2026.0828.0` も npm が黙って書き換える。
`0905` ではなく `905`、`0000` ではなく `0`。

**UTC で読む**。手元と CI で番号がぶれないため。時刻は `Date` ではなく
`Temporal.ZonedDateTime` で受け取る (Bun 1.4 / TS の `lib: esnext` 実装済み)。
どの時間帯で読むかが値そのものに入るので、`getUTC*` を呼び忘れても
ローカル時刻に落ちる、という間違え方ができない。

**代償: `^` と `~` の意味が変わる**。`^2026.828.1430` は年内なら何でも、
`~2026.828.1430` はその日のうちなら何でも許す。利用者には固定を勧める必要があり、
README に書いてある。読みやすさも落ちる (`2026.828.1430` は一見して日付に見えない)。

同じ分に 2 回出すと番号が衝突するが、npm が重複を拒否するので事故にはならない。

## ADR 19: 日付は `Type.Instant` と `Type.PlainDate` に分ける

`Type.Date` (実行時 `Date`) を廃止し、瞬間 (`Temporal.Instant`) と暦日
(`Temporal.PlainDate`) を別の型にした。

**なぜ**: Temporal には `Date` のように両方の書き方を飲み込む型が無い (実測)。

| 入力                   | `Temporal.Instant` | `Temporal.PlainDate` |
| ---------------------- | ------------------ | -------------------- |
| `2026-08-28T14:30:00Z` | 通る               | 例外                 |
| `2026-08-28`           | 例外               | 通る                 |

どちらか一方を `Type.Date` に割り当てると、もう一方の入力が黙って表現できなく
なる。`Type.*` はもともと「型を明示的に宣言する系統」なので、利用者にどちらの
意味かを言ってもらう方が筋が通る。境目は test/contract/coercion.test.ts に
表で固定してある。

**副産物**: `new Date("nope")` は Invalid Date を返して先に進んでしまうが、
Temporal は例外を投げる。読めない値が「読めたつもり」で通らない。
`min` / `max` もビルド時 (`src/declaration/parse.ts`) に解釈して弾く。

**valibot 1.1.0 に Temporal 対応は無い**。`v.instance()` で受け、境界は
`v.minValue` ではなくその型の `compare` で見る (Temporal の値を `<` で
比べると TypeError になる。実測)。

## ADR 20: 破壊的変更は、非推奨にしてから 1 年後に削除する

いきなり消さない。後方互換を保ったまま非推奨にし、移行の手順を示し、
**1 年後**に削除する。

**なぜ**: CalVer の番号は日付なので、互換性を伝えられない (ADR 18)。
`^` を付けている利用者は、番号を見ても壊れることに気づけない。
番号で伝えられない分、時間で埋め合わせる。

**忘れないための仕組み**が要る。「あとで消す」は忘れるので、期限を
`src/deprecations.ts` にデータとして置き、test/docs/deprecations.test.ts が
見張る。期限を過ぎるとテストが落ち、消すまで CI が通らない。
期限を延ばすのは決め直しなので、この ADR を書き換えることになる。

**知らせ方は 2 経路**。使う側にはビルド時の警告 (`checkDeprecations`) で
移行手順と期限を伝え、リリース時には GitHub Release の本文
(`scripts/release-notes.ts`) に破壊的変更と非推奨を出す。
`gh release create --generate-notes` は使わない。`--notes-file` と併用した
ときにどちらが残るかが保証されておらず、破壊的変更が黙って落ちうるため。

**最初の適用先は `Type.Date`** (ADR 19)。2026-08-29 に非推奨、
2027-08-29 以降に削除する。

## ADR 21: 補完の候補は CLI 自身が返す

zsh の補完は 2 つの部品で実現する。ビルドが生成する薄いシム
(`dist/completions/_<bin>`) と、ランタイムの予約コマンド `__complete`
(`src/runtime/complete.ts`)。Tab のたびにシムが
`<bin> __complete -- <words...>` を実行し、CLI が候補を stdout に
1 行 1 つ (`値<TAB>説明`) で返す。最後の語が「補完中の語」。

**なぜ問い合わせ方式か**: 候補を補完ファイルに焼き込むと、コマンドを
足すたびにファイルが変わり、zsh の compdump キャッシュを無効化する
きれいな手段がない。シムを「聞きに行くだけ」にすれば、ビルドし直した
CLI が次の Tab からそのまま反映される。**ただし反映は bundle の更新が条件**:
`decopin dev` は `.decopin/` しか更新しないので、補完に映るのは `decopin build`
の後 (シム自体は不変)。候補の情報源は argv.tsx ただ 1 つで、--help と同じ
(ADR 8)。enum は値まで補完でき、hidden は出さない。

**コマンド名は `bin` のキーから取る**: help に出す `program` は package.json の
`name` だが、利用者が実際に打つのは `bin` のキー (このリポジトリなら
name は decopin-cli、コマンドは decopin)。補完はコマンド名に付くので、
シムのファイル名と中身は `bin` から解決する (`resolveBinaryName`)。

**入口の判定は `--` まで見る**: `__complete` の 1 語目だけで判定すると、
ルートコマンドが `__complete` という文字列を位置引数に取れなくなる。
シムが必ず付ける 2 語目の `--` まで一致したときだけ補完として扱う。

**語の解釈はトークナイザを共有する**: 単独の `-` (位置引数)、`--name=value`、
束ねた alias (`-lv`)、`--no-flag` の扱いを補完側で再実装すると必ずずれる。
確定済みの語は `src/validation/tokens.ts` の `tokenize` に渡し、「補完中の語が
どの位置に落ちるか」は実入力に現れない印 (`\0`) を差し込んで同じトークナイザに
聞く (probe 方式)。

**名前の衝突は起きない**: scanner は `_` 始まりのディレクトリを共有コードの
置き場として無視する (ADR 3) ので、`app/__complete/` がルートになることはない。
予約名の検査は要らない。

**補完は決して落とさない**: 宣言が壊れていても Tab のたびにエラーが出るのは
最悪なので、`__complete` は何があっても exit 0。候補が無ければ黙って空を返し、
シェル側がファイル補完に落ちる。

**代償**: Tab のたびにプロセスが 1 回起動する (起動は 12〜14ms、ADR 12)。
体感で問題になったら、構造 (コマンド名・オプション名) だけ静的に焼いて
値だけ問い合わせるハイブリッドにする余地を残す。

## ADR 22: 動的な出力は AsyncIterable 駆動の島に限る

Next.js の PPR と同じ向きで、**静的がデフォルト、動的はオプトイン**。
`<Dynamic source={asyncIterable}>{(value) => JSX}</Dynamic>` だけが時間で
書き換わり、それ以外は従来どおり静的に流れる。ドキュメントは上から順に
flush され (ストリーミング)、島に当たったら source が尽きるまで領域を
描き換え、最後のフレームを残して続きが流れる。駆動は `present()`
(`src/renderer/present.ts`)。

**なぜ hooks ではないか**: `useState` 方式はレンダラーに再実行スケジューラを
要求し、コンポーネントが「呼ばれて JSX を返すだけの純粋なデータ」(ADR 1)
でなくなる。時間を「値の列」として渡せば、source はただの async generator
なので単体テストは generator を呼ぶだけで済む。

**島は stderr**: UNIX の慣習どおり、パイプに流れるデータは stdout、
進捗のような一時的な演出は stderr (curl / cargo と同じ)。
`cli deploy | tee log` でエスケープ列がログに混ざらない。

**TTY でないときはフレームを描かない**: パイプ・CI では途中のフレームを
出さず、source 完了時の最終フレームだけを 1 回書く。

**置ける場所はドキュメントの直下だけ**: 測ってから組むコンテナ (`<Box>` /
`<Columns>` / `<Indent>`) や `<Line>` の中の領域は消す行数の計算が壊れるので、
layout に到達した `<Dynamic>` は誤りとして落とす。

**ADR 1 の精緻化**: 「fd ごとに 1 回 write」は「静的チャンクごとに 1 回」に
広がる。各チャンクは改行で終わるので、行が割れる事故は起きない。
島の無いドキュメントは従来どおり 1 回書きのまま。

レビュー (PR #16) で固めた個々の契約 (評価の順序、repaint のライフサイクル、
シグナルでの後始末など) は、文章ではなく test/renderer/present.test.tsx に
置いてある (ADR 15)。破ればテストが気づかせてくれる。

**代償と割り切り**: リサイズ (SIGWINCH) は折り返しの変化で前の領域を正確に
消せないことがある (現フレームの描き直しだけ行う)。島の実行中に子プロセスが
直接 stderr に書くと領域が崩れる — 子プロセスの出力の取り回しは将来の課題。
永遠に待つ source を repaint 失敗で中断した場合、generator の finally は
走らない可能性がある (return を待たないため)。

## ADR 23: アニメーションは時刻ではなく描き直しの回数で進める

`<Spinner>` のコマは `Date.now()` ではなく、layout に渡る **tick**
(何回目の描き直しか) で決まる。tick を増やすのは `present()` で、
`<Dynamic>` の描き直しのたびに 1 つ進む。

**なぜ**: コンポーネントが時刻を読むと、同じ入力から同じ出力が出なくなる。
decopin のコンポーネントは「呼ばれて JSX を返すだけの純粋なデータ」(ADR 1)
なので、そこに時計を持ち込むとスナップショットのテストが書けなくなり、
`--help` のような静的な出力まで実行のたびに変わりうる。
tick なら出力は入力だけで決まり、アニメーションは repaint ループの
副産物として落ちてくる (`interval` を付ければコマが進む)。

**副産物**: 静的な出力の中の `<Spinner>` は最初のコマで止まる。
「動いていないスピナー」は紙の上では正しい絵なので、これでよい。

**`<ProgressBar>` は最初から純粋**なので tick を使わない。ただし端の扱いだけ
決めてある: 0 は空、満了だけが全部埋まり、途中は必ず 1 桁以上・width 未満。
1% で「始まった」ことが、99% で「まだ終わっていない」ことが見えるため。

## ADR 24: 起動時間は PR ごとに測り、base と同じジョブで比べる

README に貼った数字は貼った瞬間から古くなる。alias の代わりを名乗る以上
起動時間は看板なので、`.github/workflows/bench.yml` が PR ごとに測り直し、
結果を PR のコメントに貼る (毎回足さず 1 つを書き換える)。

**同じジョブで base も測る**。CI のランナーは速さが揃わないので、PR 間で
絶対値を比べても意味がない。base と PR を**同じランナーで続けて**測れば、
機械の差は引き算で消える。base のビルドに失敗したときは比較を諦めて
PR 側の数字だけ出す (計測が壊れて何も見えなくなる方が困る)。

**見出しの数字は「取り分」**: 合計から空の Bun バイナリ (ランタイムの固定費)
を引いた分。合計の 11ms のうち 4.7ms は Bun が起動するだけの分で、
そこは decopin の責任ではない。

**ゲートにしない**。閾値を置くと、ランナーのばらつきで落ちるテストになり、
やがて誰も見なくなる。数字を出して読み方 (数 % はノイズ) を添えるに留める。

**組み立ては純粋な関数に置く** (`scripts/bench-report.ts`)。退行の見え方も
base 欠損時の振る舞いも、ランナーの上で動かさずにテストできる。

## ADR 25: データは `data.tsx` で、表示と分ける

入力は宣言済み (argv / stdin / env) なのに出力は暗黙、という非対称を解く。
`data.tsx` がデータだけを作り (Remix の loader 相当)、`command.tsx` は
`data` を props で受けて表示に専念する。

**型は TypeScript の推論を借りる**。argv と違って `data.tsx` の中身は
ビルド時に評価できない (I/O を伴う)。そこで生成する型に
`Awaited<ReturnType<typeof import('../app/x/data.tsx').default>>` を書き、
戻り値の型が `command.tsx` の props にそのまま届くようにした。
TS Compiler API は要らない (JSON Schema の生成には必要になる。それは別の話)。

**`--json` は予約オプション**。`data.tsx` があるコマンドで付けると、
view を呼ばずにデータをそのまま構造化して出す。`<Json>` に渡すだけなので、
端末では色が付き、パイプでは付かない (既存の色判定に乗る)。
`data.tsx` の無いコマンドで付けたら exit 2 で「置き場所」を教える。

**パイプで自動的に JSON にはしない**。「非 TTY なら view を捨ててデータを
出す」案は捨てた。`cli user list | grep alice` のような既存のパイプが
全部壊れるため。色を落とすのは表示の調整だが、出力の**形式**を変えるのは
別物で、これは明示的に頼まれたときだけやる。

**`--json` の予約は破壊的変更**。`<Option name="json">` を宣言していた
CLI はビルドが通らなくなる。非推奨の猶予を置けない類 (予約は足した瞬間に
効く) なので、ADR 20 の経路のうち**リリースノートで知らせる**方を使う。
移行はオプション名の変更だけ。

## ADR 26: パイプを壊さないことは、覚えるのではなく掃いて確かめる

`cli stats | grep README` が通ることは CLI の一番外側の約束なのに、機能を
足すたびに壊しうる。しかも壊し方はいつも同じ形をしている: 出力の**形式**を
暗黙に変える (非 TTY なら JSON にする)、装飾を stdout に出す、画面制御を
stdout に混ぜる。

そこで機能ごとに「今回はパイプを壊していないか」を思い出すのをやめ、
test/contract/pipe-safety.test.tsx が **app/ の全コマンドを掃く**。
守る不変条件は 4 つ:

1. stdout は端末かどうかで変わらない (色 = SGR を除いて)
2. stdout に画面制御 (カーソル移動・消去・表示切替) が出ない
3. 非 TTY の stdout に ESC が 1 バイトも残らない
4. stdout は改行で終わる (空なら空)

**表に載せるのは義務**。新しいコマンドを足すと網羅の検査が落ちるので、
「このコマンドはパイプでどう見えるべきか」を決めるまで通らない
(ADR 16 で ADR に守り方を強制しているのと同じ形)。

**色だけは変わってよい**。色を落とすのは表示の調整で、形式の変更ではない。
不変条件 1 が SGR を除いて比べているのはそのため。ここを「完全一致」に
すると、正しい挙動が落ちる。

**効くことを確認済み**: 非 TTY で自動 JSON 化する変更を仕込むと 1 件、
`<Dynamic>` の描画先を stdout に変えると 14 件落ちた。

## ADR 27: `--json` に出せるかは、型ではなく実行前に見る

`JSON.stringify` は黙って壊す。関数と `undefined` は消え、`Map` / `Set` は
`{}` になり、`NaN` は `null` になる。投げるのは `bigint` だけ。`Date` /
`Temporal` は文字列になるので、宣言した型と JSON の中身が食い違う。
どれも「出してみたら欠けていた」で気づく類なので、`--json` の直前に見て
**どの経路が悪いか**を名指しして止める (`src/runtime/serializable.ts`)。

**型検査には頼らない**。当初は生成した `types.d.ts` に
`AssertJsonData<CheckJsonData<...>>` を書いて型エラーにする実装を入れたが、
**`skipLibCheck: true` があると黙って消える** (実測)。この設定はよくあるので、
利用者の tsconfig に依存する検査は約束にならない。型の計算自体は正しく
動いていたのに、エラーが報告されないという最悪の壊れ方をした。

**取りこぼしが無い方を選ぶ**。実行前の検査は、data が使われていなくても
`--json` を打てば必ず通る。型側は `data` を使っている箇所でしか効かない。
`JsonValue` 型は公開してあるので、`satisfies JsonValue` と自分で書きたい人は
書ける (強制はしない)。

**「skipLibCheck を切ればいい」で済まない**。3 つ測った結果:

1. このリポジトリで切ると、`@types/node` と `bun-types` の衝突で
   **直せないエラーが 6 件**出る (時間は 0.145s → 0.290s で問題にならない)
2. そもそも生成物が置かれるのは**利用者のリポジトリ**で、そこの tsconfig は
   こちらが決められない。`.d.ts` に埋める限り約束にならない
3. 生成物を `.ts` にすれば skipLibCheck は効かなくなる (実測)。ただし
   制約に `JsonValue` を使うと **interface が索引シグネチャを持たない**ため
   正常な `interface Report { count: number }` まで弾く。キーを 1 つずつ辿る
   `IsJsonSerializable` なら interface も通るが、今度はエラー文に
   どのプロパティが悪いか出ない。両立させようとすると TS が
   「循環した制約」か「既定値を検証できない」で止まる

型で正確にやろうとするほど、偽陽性か不親切な文面のどちらかに倒れる。

**偽陽性の本当の害は、黙らされること**。正しいコードが弾かれた利用者は
`as never` や `@ts-expect-error` で通す。その抑制は偽陽性より長生きし、
そこを通る**本物の問題まで隠す**。検査が無い状態より悪くなる。
実行前検査は偽陽性が出ないので、黙らせる動機がそもそも生まれない。

**1 件で止める**。直す順番は上から下に決まっていることが多いので、
最初に見つけた経路だけを出す。

**素の object だけを通す**: プロトタイプが `Object.prototype` か `null` の
ものだけ。クラスのインスタンスを弾くのは、`toJSON` があってもなくても
「型に書いてある形とは違うものが出る」ため。

## ADR 28: `output.tsx` があれば、それが出力の正になる

`data.tsx` の戻り値の形を `Type.*` で宣言できるようにした。あれば
**その宣言が正**で、2 つのことが起きる:

- `command.tsx` の `data` の型は、推論ではなく宣言から作る
- 実行時に data を検証し、外れたら表示にも `--json` にも流さない

**自分のコードなのに検証するのはなぜか**。`data.tsx` が
`await res.json() as User[]` のように外から来た値を返すとき、TypeScript は
書いたとおりに信じるだけで、実物が違っても気づけない。境界で確かめるのは
parse-don't-validate の要領で、ここが唯一の境界になる。

**宣言が無ければ縛らない**。`output.tsx` を置いていないコマンドは
今までどおり推論で型が付き、検証もされない。全部に強制すると
`<Type.Object>` の記述量 (3 階層で 9 行) が割に合わない場面が出る。

**`schema` prop の逃げ道**は stdin と同じ形にした (ADR 9 の前例)。
込み入った形は valibot を直接渡せる。`Type.*` を主にするのは
ADR 10 のとおりだが、逃げ道が無いと表現力で詰まる。

**JSON Schema はまだ出さない**。`TypeNode` から JSON Schema への変換は
`toTypeText` と同じ形の switch で書けるが、読む相手 (MCP 輸出) がまだ
無い。使われない生成物は朽ちるので、輸出を作るときに一緒に足す。

## ADR 29: `--json` を頼まれたら、失敗も構造化して返す

`--json` は「機械が読む」という宣言なので、成功だけ JSON にして失敗を
人間向けの文面で返すとパーサが壊れる。`--json` が付いていれば、失敗は
stderr に `{ "error": { code, message, exitCode, issues? } }` で出す。

**`error.tsx` は通さない**。あれは人が読むための表示なので、JSON を
求められた場面では飛ばす。`command.tsx` を飛ばすのと同じ理由。

**stdout は空のまま**。失敗時に stdout へ書かないのは元からの約束で、
test/contract/pipe-safety.test.tsx が失敗する `--json` も掃いて固定した。

**`code` は分類、`message` は文面**。機械が分岐する先を文面にすると、
言い回しを直した瞬間に相手が壊れる。いまの `code` は枠組み側の分類
(`validation` / `runtime` / `stdin` / `env` / `unknown`) で、
利用者が自分のコードを宣言する仕組みは次の段。

**`exitCode` も入れる**。パイプで受けた側はシェルの `$?` を見られない
ことがある (`cmd | jq` では jq の終了コードが返る) ので、本文にも入れておく。

**sysexits.h (EX_SOFTWARE = 70 など) は採らない**。BSD 由来で普及も
一様ではなく、いまの `1` = 実行時の失敗 / `2` = 使い方の誤りの方が
現代の CLI の慣習に近い。番号を変えるのは利用者の `if [ $? -eq 1 ]` を
黙って壊すので、得るものが無い限りやらない。

## ADR 30: 型の一覧を宣言させるより、よくある形を用意する

エラー契約の検討で、最初は「想定できる失敗の union を宣言させる」方向
(Effect の `E` チャネル / Java の checked exception) を考えた。調べた結果、
**そちらは採らない**。

**言語側の結論**: 二層に分ける (想定できる / できない) こと自体は
Java・Rust・Effect・Zig・Go が全部同じで異論が無い。割れるのは
**一覧を誰が書くか**で、Java の checked exception は書く負担で崩れた
(`throws Exception` / 空の catch / 包み直し)。Kotlin と Scala は捨て、
C# は採らなかった。typed throws を**新たに入れた** Swift 6 でさえ、
公式の指針は「untyped がほとんどのコードで良い既定のまま」「将来変えたく
ない契約に自分を縛る」「case を足すのは source-breaking」である。

**CLI では網羅性が強制できない**という非対称もある。呼び出し側はシェル
スクリプトやエージェントなので、コードを足しても*コンパイルは壊れず*、
実行時に未処理の分岐に落ちるだけ。つまり宣言の見返りが小さい。

**代わりにやること**: 利用者がライブラリに期待するのは型システムではなく
**「よくある形が既に正しく用意されている」**こと。Next.js の `notFound()` が
その形なので、そこに寄せた。

- `notFound()` — どこからでも投げれば一番近い `not-found.tsx` が表示される。
  `available` を渡せば「もしかして」は**自動で付く**。終了コードも決まる
- `not-found.tsx` を継承可能にした (`error.tsx` と同じ近い順)。
  コロケーションが効くのがこの形の要
- `help()` — そのままでは進めないときに、このコマンドの使い方を見せて
  打ち切る。`--help` と同じものを組み立てるが、**求められて出すのでは
  ない**ので stderr + exit 2 (ルーティングの表と揃う)。理由の一行を
  `message` で先に出せる
- `<DidYouMean>` と `closest()` を公開した。編集距離は枠組みが未知の
  コマンドに使っていたのに、利用者からは呼べなかった
- 一覧を宣言していない失敗は今までどおり `unknown` に落ちる (ADR 29)。
  `thiserror` (公開面は精密) と `anyhow` (その下は不透明) の使い分けと同じ

**`notFound()` は失敗ではない**。「探したものが無い」は正常な結末なので、
終了コードは既定 1 (使い方の誤りの 2 ではない)。未知のコマンドだけは
利用者の打ち間違いなので 2 のまま。

**props で見分ける**。`what` が `'command'` か、`notFound({ what: 'user' })` で
渡した値か。1 つの `not-found.tsx` が両方に使えるよう、`requested` /
`available` / `suggestion` は共通の形にした。

## ADR 31: 「環境が整っていない」は、直し方まで含めて 1 つの形にする

認証が切れている・必要なコマンドが入っていない、は CLI が必ず通る道で、
どちらも同じ骨格を持つ: **何が足りないか + 打てば直るコマンド**。
`authRequired()` と `missingTool()` を用意した (ADR 30 の続き)。

**新しい機構は足さない**。`notFound()` / `help()` と違って、これは本物の
失敗なので `error.tsx` の経路にそのまま乗せる。やったのは 3 つだけ:

- `ErrorKind` に `auth` / `missing-tool` を足した → `error.tsx` が場合分けでき、
  ADR 29 の `--json` の `code` にもそのまま出る
- `CliError` に `hints` を足した → 既定表示では message の下に並ぶ
- `--json` のペイロードにも `hints` を出す

**`hints` を機械にも渡すのが要**。人が読むなら「`gh auth login` を打て」で
済むが、エージェントは**次の一手を決める**ために同じ情報が要る。
`code: "auth"` で「認証だ」と分かり、`hints` で「何をすれば直るか」が分かる。

**終了コードは 1 のまま**。認証切れ専用の番号を作りたくなるが、ADR 29 で
決めたとおり番号は変えない。分岐先は `code` であって番号ではない。

**独自の `error.tsx` が hints を捨てうる**のは避けられない (表示は利用者の
もの)。せめて例アプリの `global-error.tsx` で「直し方は落とさない」書き方を
実演しておく。組み込みの既定表示は最初から出す。

## ADR 32: 副作用は申告させず、ビルド時に数える

コマンド 1 つの入口が規約で決まっていて、ビルド時に閉じた世界を作る —
この構造があるから、**そのコマンドから到達しうる sink** を機械が数えられる。
自己申告 (Effect の Requirements 的なやり方) だと宣言と実装がずれるが、
数えるならずれようがない。

**判定は三値**。`none` / `detected` / `unknown`。狙いは `none`、つまり
**「無いことの証明」**。`eval` や解決できない import に当たったら `unknown` に
落として、`none` を名乗らない。健全性を精度より優先する。

**モジュールグラフの粒度から始める** (ロードマップの第一弾スコープ)。
関数粒度の解析はしない。ただし `import { readFile } from 'node:fs'` の
**名前までは見る** — ここを見ないと fs を触る全コマンドが読み書き両方
`detected` になり、判定が意味を失う。名前空間 import は絞れないので両方採る。

**fact が要る**。中身を歩かずに「こう」と決めてよいものの表。最初の登録は
**枠組み自身**で、これが無いと全コマンドが `process.mutate: detected` になった
(実測)。`decopin-cli` の中の `process.exit` は終了コードを返す仕組みであって、
コマンドの副作用ではない。

**実際に動かして 3 つ直した**:

- `Bun.Transpiler` は tsconfig を読まないので、JSX 変換が `react` の
  import を勝手に足す。書いた人の依存ではないので数えない
- `Bun.resolveSync` は**絶対パスのディレクトリ**を要求する。相対のまま渡すと
  素の相対 import まで「解決できない」= `unknown` に落ちた
- `Bun.$\`ls\`` を「バックティックが続くこと」で見ていたが、文字列を落とす
  前処理でバックティックごと消えていて検出できていなかった。参照だけを見る

**まず出す先はビルドの出力**。MCP のヒントに変換するのはロードマップ通りだが、
読む相手 (MCP 輸出) がまだ無い。使われない生成物は朽ちるので、輸出を作るときに
一緒に足す (ADR 28 と同じ判断)。

## 実測メモ: この設計で踏んだ落とし穴

後から同じところを踏まないために残す。

- **`v.pipe()` は基底スキーマの浅いコピー + `pipe` 配列**で、入れ子の pipe は
  平坦化されない。1 段しか見ないと
  `v.pipe(v.pipe(v.string(), v.transform(Number)), v.minValue(0))` の transform を
  取りこぼして誤った型 (`string`) を出す。`pipe[0]` を再帰的に辿る必要がある
- **Bun は SIGINT のハンドラが無いと exit 0 で終わる**。シェル慣習の 130 には
  ならないので、生成する `entry.ts` で明示的に配線する
- **`tsconfig.json` はコメントと末尾カンマを許す (JSONC)**。素の `JSON.parse` は
  実在する設定で落ちる
- **利用者のプロジェクトに `jsxImportSource` が無いと React を探しに行く**。
  `Could not resolve: react/jsx-runtime` という原因の分かりにくい失敗になるので、
  ビルド時に警告する
- **`String.length` は表示幅と合わない**。日本語 2 桁・絵文字 2 桁・結合文字 0 桁を
  自前で数えないと罫線や表がずれる
- **生成されるファイルは利用者のリポジトリに残る**。`.decopin/` のヘッダーや
  コメントも「フレームワークが出すもの」なので英語で書く (ADR 14 のリントが
  この不整合を実際に見つけた)

---

## 未決 / 保留

**決めていないこと**をここに置く。ADR ではないので、`test/docs/decisions.test.ts`
の分類の対象にはならない。決まったら ADR に昇格させる。

- 型が**古い** (生成済みだが宣言と食い違う) 場合は素直に型エラーになる。未生成の
  ときのフォールバックは実装したが、「古い」と「未生成」は区別できていない。
  `decopin dev` を回す運用でしのいでいる
- `<Type.Object>` / `<Type.Field>` は 3 階層で 9 行になる。深い JSON では
  `schema` prop を勧めるが、内省できないノードがあると型が `unknown` に落ちる
- `<Arg variadic>` と `<Type.Array>` は別物 (前者は位置引数を複数取る、後者は
  1 つの値が配列) だが、生成される型が両方 `string[]` になり混同しやすい
- ADR 11 の代償 (env / argv の検証エラーが middleware を通らない) が実用上
  問題になるなら、計測・ロギングの責務を `global-error.tsx` に寄せる形を検討する
- 起動時間は総時間 12〜14ms で、目標の 10ms は未達 (ADR 12)。さらに削るには
  6〜8ms が何に使われているかのプロファイルが要る
- middleware の `args` / `options` は `Record<string, unknown>` のまま。middleware は
  複数コマンドにまたがるので、生成された型を当てるには「そのディレクトリ以下の
  コマンドの union」が必要
- `<Table>` のセルは文字列だけで JSX を入れられない。セルに色を付けたい要望が
  出たら `Cell` に `{ text, style }` の形を足す
